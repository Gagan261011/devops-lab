---
- name: Deploy Java CRUD Application
  hosts: app_server
  become: yes
  gather_facts: yes
  
  vars:
    app_user: ubuntu
    app_dir: /opt/app
    app_jar_name: app.jar
    app_port: 8080
    java_home: /usr/lib/jvm/java-17-openjdk-amd64
  
  roles:
    - app_deploy
